{
  "name": "hardhat-project",
  "scripts": {
    "deploy": "npx hardhat run scripts/deploy.js",
    "update-contracts": "npx hardhat clean && npx hardhat compile && npx hardhat run scripts/update-contracts.js"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^1.0.2",
    "hardhat": "^2.10.1"
  }
}
